# Common SQL Mistakes

- Forgetting commas in SET.
- Using two WHERE clauses.
- WHERE id = 1,2,3 instead of IN (1,2,3).
- Forgetting quotes around text values.
- Trying to assign multiple values to one column.
- Misspelling SQL keywords (e.g. ALTER).
- Using old column/table names after renaming.
