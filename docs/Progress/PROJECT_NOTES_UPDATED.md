# Phase 4 Safety Rules

1. Use a separate practice table for structure-change lessons.
2. Check the structure with `.schema`.
3. Check the data with `SELECT *`.
4. Remember:
   - ALTER TABLE changes table structure.
   - UPDATE and DELETE change data.
5. Verify results after every change.
